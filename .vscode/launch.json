{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lua-local",
            "request": "launch",
            "name": "Debug",
            "program": {
                "command": "skynet"
            },
            "args": [
                "${workspaceFolder}"
            ],
            "env": {
                "LOCAL_LUA_DEBUGGER_VSCODE": "1",
            }
        }
    ]
}